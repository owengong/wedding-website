<section class="page-hero">
  <p class="eyebrow"><%= eyebrow %></p>
  <h1 class="display"><%= heading %></h1>
  <% if (typeof lede !== 'undefined' && lede) { %><p class="lede"><%= lede %></p><% } %>
</section>
